// 0-list_databases
// Lists all databases in MongoDB

// Switch to the admin database
use admin;

// Run the listDatabases command
var result = db.adminCommand({ listDatabases: 1 });

// Print all database names
print("Databases:");
result.databases.forEach(function(database) {
    print("- " + database.name);
});
